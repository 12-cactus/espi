import schedule from '../../src/core/schedule';

describe('Testing GPT', () => {
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  const a = schedule.sendRegards;
  it('under construction', async () => {
    expect(true).toBe(true);
  });
});
