import holidays from '../../src/core/holidays';

describe('Testing GPT', () => {
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  const a = holidays.holidaysAR;
  it('under construction', async () => {
    expect(true).toBe(true);
  });
});
